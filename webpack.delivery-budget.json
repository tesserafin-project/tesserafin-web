{
    "$comment": "Aggregate initial/start-up delivery contract. The INDIVIDUAL main-bundle budget is not here: it stays in webpack.performance-budget.json, which webpack.prod.js enforces during the build itself. Enforced by scripts/verify-delivery-budget.mjs; see docs/tesserafin/web-delivery-budget.md. Raising a ceiling is a deliberate edit to this file, reviewed in a pull request with before/after numbers. Nothing rewrites it automatically - there is no --update-baseline.",
    "statsSchemaVersion": 1,
    "htmlEntrypoints": ["main.tesserafin", "serviceworker"],
    "htmlEntrypointsNote": "The entrypoints html-webpack-plugin injects into index.html (webpack.common.js `chunks`). The theme entrypoints (themes/*) are deliberately absent: their stylesheets are fetched by the theme manager at runtime, one at a time, and are not part of what a cold visitor downloads first. The verifier cross-checks this list against the tags actually injected and fails if they diverge.",
    "bootModules": ["./index.jsx"],
    "bootModulesNote": "Modules whose `import()` calls run unconditionally at start-up. Their chunks are counted in the `startup` tier so that the budget cannot be dodged by moving a static start-up import behind `import()`: the bytes change tier, the total does not. Route-level `lazy()` boundaries are NOT listed - those are async on purpose and are governed by protectedModulePatterns instead.",
    "countedAssetExtensions": [".js", ".css"],
    "countedAssetExtensionsNote": "Anything else appearing in the counted set (a font, an image, a wasm module promoted into an initial chunk) is a hard failure, not a silent exclusion: it has to be classified deliberately here first.",
    "startupGzipCeilingBump": "904192 -> 905216 in tesserafin-web #139 (M3). #139 gate 4 requires primary navigation to visibly change when the household chooses content-pack-first browsing, so `UserViewNav` — which lives in main.tesserafin.bundle.js — gains one conditional nav entry. Measured from two clean production builds, base 5b119a0356 vs head: startup.gzipBytes 904147 -> 904314 (+167), startup.rawBytes 3221806 -> 3222535 (+729), startup.brotliBytes 571650 -> 571796 (+146). Every other ceiling still passes untouched, and BOTH asset counts are unchanged (initial 25, startup 45). The addition was already minimised: the resolver module is not imported (webpack tree-shook it to the same bytes either way), the comparison is inlined against the generated enum, and the label reuses the `ContentPacks` string M2 already ships — a duplicate `HeaderFirstRunContentPacks` string was deleted rather than added. The new ceiling is the measurement rounded up to the next whole KiB, per the `rounding` rule above.",
    "startupAssetCountBump": "44 -> 45 in tesserafin-web #129 Step 1b, and NOT a byte increase: every byte total fell. Base 61ee617b vs head, two clean production builds with their own node_modules: startup.rawBytes 3223410 -> 3220153, gzip 903959 -> 903814, brotli 571450 -> 571397; initial.rawBytes 2129431 -> 2126101, and initial.assetCount unchanged at 25. The extra file is inside the SAME boot-time chunk group, ./components/nowPlayingBar/nowPlayingBar, where splitChunks emitted three files (49926.chunk.js, 49926.css, 93119.chunk.js) where it previously emitted two (77423.chunk.js, 77423.css). That is the mechanical split this file's own assetCountNote describes.",
    "assetCountNote": "initial.assetCount is a stable, small set - treat any movement as a real change. startup.assetCount is the metric most likely to need a mechanical bump: the tier counts whole chunk GROUPS, and splitChunks will happily extract a newly shared module out of one of them into two files. A count bump whose byte totals are unchanged is a chunk split, not growth; say so in the pull request and move on.",
    "rounding": "Every byte ceiling is the measurement rounded UP to the next whole KiB (1024 B). Asset counts take no margin: the measured count is the ceiling. The KiB margin exists only to absorb the handful of bytes that move when __COMMIT_SHA__ / __JF_BUILD_VERSION__ differ between a local build and a CI build (webpack.common.js, .github/workflows/__package.yml) - it is not headroom for features.",
    "baseline": {
        "commit": "1cb72daa15ccbddaf8e095c0d541e04911cc17d8",
        "note": "Measured on tesserafin-web main at the squash merge of PR #130, from two clean production builds with identical results. See docs/tesserafin/web-delivery-budget.md for the full asset table."
    },
    "compression": {
        "gzip": "node:zlib gzipSync, level 9, zlib defaults otherwise",
        "brotli": "node:zlib brotliCompressSync, BROTLI_PARAM_QUALITY 11, no BROTLI_PARAM_SIZE_HINT",
        "method": "each asset compressed separately, sizes summed - assets are separate HTTP responses with separate compression contexts. Nothing compressed here is written to dist/."
    },
    "budgets": {
        "initial": {
            "rawJsBytes": 1957888,
            "rawCssBytes": 172032,
            "rawBytes": 2129920,
            "gzipBytes": 544768,
            "brotliBytes": 459776,
            "assetCount": 25
        },
        "startup": {
            "rawBytes": 3223552,
            "gzipBytes": 905216,
            "brotliBytes": 572416,
            "assetCount": 45
        }
    },
    "protectedModulePatterns": [
        {
            "id": "theme-studio",
            "pattern": "^\\./apps/modern/(features/themeStudio|routes/user/themeStudio)/",
            "reason": "Theme Studio is an authoring tool reached from Display preferences, not a viewer destination (tesserafin-web #119). It must stay behind its async route: an ordinary viewer who never opens it must never download it."
        },
        {
            "id": "theme-authoring-validation",
            "pattern": "^\\./themes/platform/(validateManifest|validateTokens)",
            "reason": "Manifest and token validation is authoring/compiler-side work (#115, #117). The viewer resolves an already-valid presentation; it does not validate one at boot."
        },
        {
            "id": "item-details",
            "pattern": "^\\./apps/modern/(features/details|routes/details)",
            "reason": "Item Details is the largest single viewer route (tesserafin-web #129 Step 1b). It replaced a view-manager controller that was already async, and it must stay async: a cold visitor who never opens an item must never download it. Navigating to /details is what first requests this chunk."
        }
    ],
    "acceptanceRuleForItemDetails": "tesserafin-web #129 Step 1b, SATISFIED: the modern Item Details route is registered through ASYNC_USER_ROUTES / toAsyncPageRoute in both route families, and the `item-details` protectedModulePatterns entry above enforces its absence from the initial and start-up graphs. No ceiling was raised for it."
}
